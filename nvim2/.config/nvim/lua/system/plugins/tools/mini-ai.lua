return {
	"echasnovski/mini.ai",
	event = "VeryLazy",
	opts = {
		n_lines = 500,
		-- a = around, i = inside
		-- adds: q (any quote), ? (user-prompted), and treesitter-based f/c/a text objects
		custom_textobjects = nil,
	},
}
